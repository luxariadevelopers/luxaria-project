import { ModuleRecordPage } from '@/quick-search';

/** Landing for quick-search booking numbers (`booking.view`). Full UI later. */
export function BookingsPage() {
  return (
    <ModuleRecordPage
      moduleLabel="Booking"
      emptyHint="Booking UI will be implemented in a later phase. Quick search can open a permitted booking number here."
    />
  );
}
