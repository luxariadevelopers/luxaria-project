import { ModuleRecordPage } from '@/quick-search';

/** Landing for quick-search vendor hits (`vendor.view`). Full UI later. */
export function VendorsPage() {
  return (
    <ModuleRecordPage
      moduleLabel="Vendor"
      emptyHint="Vendor master UI will be implemented in a later phase. Quick search can open a permitted vendor here."
    />
  );
}
