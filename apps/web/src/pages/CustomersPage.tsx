import { ModuleRecordPage } from '@/quick-search';

/** Landing for quick-search customer hits (`customer.view`). Full UI later. */
export function CustomersPage() {
  return (
    <ModuleRecordPage
      moduleLabel="Customer"
      emptyHint="Customer master UI will be implemented in a later phase. Quick search can open a permitted customer here."
    />
  );
}
