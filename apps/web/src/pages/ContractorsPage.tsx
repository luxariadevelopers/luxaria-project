import { ModuleRecordPage } from '@/quick-search';

/** Landing for quick-search contractor hits (`contractor.view`). Full UI later. */
export function ContractorsPage() {
  return (
    <ModuleRecordPage
      moduleLabel="Contractor"
      emptyHint="Contractor master UI will be implemented in a later phase. Quick search can open a permitted contractor here."
    />
  );
}
