/**
 * Route path helper — registry path in `navigation/routeRegistry.ts`.
 *
 * Group: **administration** (`/administration/approval-workflows`)
 */
export const APPROVAL_WORKFLOW_ROUTES = {
  home: '/administration/approval-workflows',
} as const;
