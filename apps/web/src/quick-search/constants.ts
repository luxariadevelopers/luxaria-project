/** Minimum characters before any module list search runs. */
export const QUICK_SEARCH_MIN_LENGTH = 2;

/** Debounce for the command palette query input. */
export const QUICK_SEARCH_DEBOUNCE_MS = 300;

/** Max hits requested per module list endpoint. */
export const QUICK_SEARCH_LIMIT = 5;
